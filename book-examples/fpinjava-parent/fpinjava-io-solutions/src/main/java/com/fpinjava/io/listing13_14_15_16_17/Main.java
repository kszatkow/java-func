package com.fpinjava.io.listing13_14_15_16_17;


public class Main {

  public static void main(String... args) {
    // IO.forever(Console.printLine("Witaj...")).run();

    // IO program = IO.forever(IO.unit("Witaj ponownie!")
    //                           .flatMap(Console::printLine));
    // program.run();

    // IO.repeat(100000, Console.printLine("Witaj...")).run();

    // IO.doWhile(Console.printLine("Witaj..."), x -> IO.unit(true)).run();
  }
}
